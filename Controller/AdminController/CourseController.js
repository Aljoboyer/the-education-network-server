const CourseModel = require("../../Models/sharedModels/course.js");

exports.create = async (req, res) => {
    console.log(req)
    // const data = new CourseModel(req) 
    // try {
    //    const saving = await data.save() 
    //    res.json(saving)
    // } catch (error) { 
    //     console.log('error -->', error)
    // }
}